# API Keys for authentication
# Multiple keys can be separated by commas
API_KEYS=your-api-key-here,another-api-key

# User Agent for weather API requests
USER_AGENT=weather-mcp-server/1.0

# Environment (production)
ENVIRONMENT=production

# Log level
LOG_LEVEL=INFO

# Note: This is a production-ready configuration
# For local development, you can run the service directly with: uv run fastapi dev main.py 